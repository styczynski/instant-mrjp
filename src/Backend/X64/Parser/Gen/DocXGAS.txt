The Language X64GAS
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of X64GAS==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




Label literals are recognized by the regular expression
`````(["'_"] | digit | letter)+`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in X64GAS are the following:
  | ``CALL`` | ``addb`` | ``addl`` | ``addq``
  | ``andb`` | ``andl`` | ``andq`` | ``cdq``
  | ``cmpb`` | ``cmpl`` | ``cmpq`` | ``idivb``
  | ``idivl`` | ``idivq`` | ``imulb`` | ``imull``
  | ``imulq`` | ``jmp`` | ``jz`` | ``leab``
  | ``leal`` | ``leaq`` | ``leave`` | ``movb``
  | ``movl`` | ``movq`` | ``negb`` | ``negl``
  | ``negq`` | ``pop`` | ``push`` | ``ret``
  | ``salb`` | ``sall`` | ``salq`` | ``sarb``
  | ``sarl`` | ``sarq`` | ``sete`` | ``setg``
  | ``setge`` | ``setl`` | ``setle`` | ``setne``
  | ``subb`` | ``subl`` | ``subq`` | ``testb``
  | ``testl`` | ``testq`` | ``xchgb`` | ``xchgl``
  | ``xchgq`` | ``xorb`` | ``xorl`` | ``xorq``

The symbols used in X64GAS are the following:
  | .section | .rodata | .text | .global
  | : | .string | .quad | .long
  | $ | .extern | , | *
  | ( | ) | (%RIP) | %RAX
  | %RBX | %RCX | %RDX | %RDI
  | %RSI | %RSP | %RBP | %R8
  | %R9 | %R10 | %R11 | %R12
  | %R13 | %R14 | %R15 | %EAX
  | %EBX | %ECX | %EDX | %EDI
  | %ESI | %ESP | %EBP | %R8D
  | %R9D | %R10D | %R11D | %R12D
  | %R13D | %R14D | %R15D | %AX
  | %BX | %CX | %DX | %DI
  | %SI | %SP | %BP | %R8W
  | %R9W | %R10W | %R11W | %R12W
  | %R13W | %R14W | %R15W | %AL
  | %BL | %CL | %DL | %DIL
  | %SIL | %SPL | %BPL | %R8B
  | %R9B | %R10B | %R11B | %R12B
  | %R13B | %R14B | %R15B |

===Comments===
Single-line comments begin with #.There are no multiple-line comments in the grammar.

==The syntactic structure of X64GAS==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //AsmProgram// | -> | //[Directive]// //SectionData// //SectionCode//
  | //[AsmInstr]// | -> | **eps**
  |  |  **|**  | //AsmInstr// //[AsmInstr]//
  |  |  **|**  | **eps**
  |  |  **|**  | //AsmInstr// //[AsmInstr]//
  | //SectionData// | -> | ``.section`` ``.rodata`` //[AsmDataDef]//
  | //SectionCode// | -> | ``.section`` ``.text`` //[AsmInstr]//
  | //AsmDataDef// | -> | ``.global`` //Label//
  |  |  **|**  | //Label// ``:`` //[Data]//
  | //[AsmDataDef]// | -> | **eps**
  |  |  **|**  | //AsmDataDef// //[AsmDataDef]//
  | //Data// | -> | ``.string`` //String//
  |  |  **|**  | ``.quad`` //DataConst//
  |  |  **|**  | ``.long`` //DataConst//
  | //[Data]// | -> | **eps**
  |  |  **|**  | //Data// //[Data]//
  | //DataConst// | -> | ``$`` //Integer//
  |  |  **|**  | //Label//
  | //Directive// | -> | ``.extern`` //Label//
  | //[Directive]// | -> | **eps**
  |  |  **|**  | //Directive// //[Directive]//
  | //AsmInstr// | -> | //Label// ``:``
  |  |  **|**  | ``addq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``andq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``cmpq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``imulq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``leaq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``movq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``subq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``testq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``xorq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``xchgq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``salq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``sarq`` //Source64// ``,`` //Target64//
  |  |  **|**  | ``addl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``andl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``cmpl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``imull`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``leal`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``movl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``subl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``testl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``xorl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``xchgl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``sall`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``sarl`` //Source32// ``,`` //Target32//
  |  |  **|**  | ``addb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``andb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``cmpb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``imulb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``leab`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``movb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``subb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``testb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``xorb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``xchgb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``salb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``sarb`` //Source16// ``,`` //Target16//
  |  |  **|**  | ``negq`` //Target64//
  |  |  **|**  | ``idivq`` //Target64//
  |  |  **|**  | ``negl`` //Target32//
  |  |  **|**  | ``idivl`` //Target32//
  |  |  **|**  | ``negb`` //Target16//
  |  |  **|**  | ``idivb`` //Target16//
  |  |  **|**  | ``CALL`` //Label//
  |  |  **|**  | ``CALL`` ``*`` //Integer// ``(`` //Reg64// ``)``
  |  |  **|**  | ``pop`` //Reg64//
  |  |  **|**  | ``push`` //Reg64//
  |  |  **|**  | ``leave``
  |  |  **|**  | ``ret``
  |  |  **|**  | ``cdq``
  |  |  **|**  | ``sete`` //Reg8//
  |  |  **|**  | ``setg`` //Reg8//
  |  |  **|**  | ``setge`` //Reg8//
  |  |  **|**  | ``setl`` //Reg8//
  |  |  **|**  | ``setle`` //Reg8//
  |  |  **|**  | ``setne`` //Reg8//
  |  |  **|**  | ``jmp`` //Label//
  |  |  **|**  | ``jz`` //Label//
  | //Source64// | -> | //Integer//
  |  |  **|**  | //Reg64//
  |  |  **|**  | //Integer// ``(`` //Reg64// ``)``
  |  |  **|**  | //Label//
  |  |  **|**  | //Label// ``(%RIP)``
  |  |  **|**  | //Integer// ``(`` //Reg64// ``,`` //Reg64// ``,`` //Integer// ``)``
  | //Target64// | -> | //Reg64//
  |  |  **|**  | //Integer// ``(`` //Reg64// ``)``
  |  |  **|**  | //Integer// ``(`` //Reg64// ``,`` //Reg64// ``,`` //Integer// ``)``
  | //Reg64// | -> | ``%RAX``
  |  |  **|**  | ``%RBX``
  |  |  **|**  | ``%RCX``
  |  |  **|**  | ``%RDX``
  |  |  **|**  | ``%RDI``
  |  |  **|**  | ``%RSI``
  |  |  **|**  | ``%RSP``
  |  |  **|**  | ``%RBP``
  |  |  **|**  | ``%R8``
  |  |  **|**  | ``%R9``
  |  |  **|**  | ``%R10``
  |  |  **|**  | ``%R11``
  |  |  **|**  | ``%R12``
  |  |  **|**  | ``%R13``
  |  |  **|**  | ``%R14``
  |  |  **|**  | ``%R15``
  | //Source32// | -> | //Integer//
  |  |  **|**  | //Reg32//
  |  |  **|**  | //Integer// ``(`` //Reg32// ``)``
  |  |  **|**  | //Label//
  |  |  **|**  | //Label// ``(%RIP)``
  |  |  **|**  | //Integer// ``(`` //Reg32// ``,`` //Reg32// ``,`` //Integer// ``)``
  | //Target32// | -> | //Reg32//
  |  |  **|**  | //Integer// ``(`` //Reg32// ``)``
  |  |  **|**  | //Integer// ``(`` //Reg32// ``,`` //Reg32// ``,`` //Integer// ``)``
  | //Reg32// | -> | ``%EAX``
  |  |  **|**  | ``%EBX``
  |  |  **|**  | ``%ECX``
  |  |  **|**  | ``%EDX``
  |  |  **|**  | ``%EDI``
  |  |  **|**  | ``%ESI``
  |  |  **|**  | ``%ESP``
  |  |  **|**  | ``%EBP``
  |  |  **|**  | ``%R8D``
  |  |  **|**  | ``%R9D``
  |  |  **|**  | ``%R10D``
  |  |  **|**  | ``%R11D``
  |  |  **|**  | ``%R12D``
  |  |  **|**  | ``%R13D``
  |  |  **|**  | ``%R14D``
  |  |  **|**  | ``%R15D``
  | //Source16// | -> | //Integer//
  |  |  **|**  | //Reg16//
  |  |  **|**  | //Integer// ``(`` //Reg16// ``)``
  |  |  **|**  | //Label//
  |  |  **|**  | //Label// ``(%RIP)``
  |  |  **|**  | //Integer// ``(`` //Reg16// ``,`` //Reg16// ``,`` //Integer// ``)``
  | //Target16// | -> | //Reg16//
  |  |  **|**  | //Integer// ``(`` //Reg16// ``)``
  |  |  **|**  | //Integer// ``(`` //Reg16// ``,`` //Reg16// ``,`` //Integer// ``)``
  | //Reg16// | -> | ``%AX``
  |  |  **|**  | ``%BX``
  |  |  **|**  | ``%CX``
  |  |  **|**  | ``%DX``
  |  |  **|**  | ``%DI``
  |  |  **|**  | ``%SI``
  |  |  **|**  | ``%SP``
  |  |  **|**  | ``%BP``
  |  |  **|**  | ``%R8W``
  |  |  **|**  | ``%R9W``
  |  |  **|**  | ``%R10W``
  |  |  **|**  | ``%R11W``
  |  |  **|**  | ``%R12W``
  |  |  **|**  | ``%R13W``
  |  |  **|**  | ``%R14W``
  |  |  **|**  | ``%R15W``
  | //Source8// | -> | //Integer//
  |  |  **|**  | //Reg8//
  |  |  **|**  | //Integer// ``(`` //Reg8// ``)``
  |  |  **|**  | //Label//
  |  |  **|**  | //Label// ``(%RIP)``
  |  |  **|**  | //Integer// ``(`` //Reg8// ``,`` //Reg8// ``,`` //Integer// ``)``
  | //Target8// | -> | //Reg8//
  |  |  **|**  | //Integer// ``(`` //Reg8// ``)``
  |  |  **|**  | //Integer// ``(`` //Reg8// ``,`` //Reg8// ``,`` //Integer// ``)``
  | //Reg8// | -> | ``%AL``
  |  |  **|**  | ``%BL``
  |  |  **|**  | ``%CL``
  |  |  **|**  | ``%DL``
  |  |  **|**  | ``%DIL``
  |  |  **|**  | ``%SIL``
  |  |  **|**  | ``%SPL``
  |  |  **|**  | ``%BPL``
  |  |  **|**  | ``%R8B``
  |  |  **|**  | ``%R9B``
  |  |  **|**  | ``%R10B``
  |  |  **|**  | ``%R11B``
  |  |  **|**  | ``%R12B``
  |  |  **|**  | ``%R13B``
  |  |  **|**  | ``%R14B``
  |  |  **|**  | ``%R15B``



%% File generated by the BNF Converter (bnfc 2.9.4).
